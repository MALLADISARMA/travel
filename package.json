{
  "name": "travel-mern-app",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run dev && cd ../admin && npm install && npm run dev",
    "start": "npm start server"
  }
}
